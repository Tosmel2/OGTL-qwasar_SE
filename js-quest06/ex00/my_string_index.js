function my_string_index(myString, myLetter) {
  for (let i = 0; i < myString.length; i++) {
      return myString.indexOf(myLetter);
  };
  // console.log(myString.indexOf(myLetter));
};
// my_string_index("hello", "l");